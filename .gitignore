.idea
.vscode
.DS_Store
node_modules
dist
**/archive.tar.gz
**/bundle.js*

# Secrets — never commit
.env
.env.local
.env.production
.env.*.local

# Runtime state
backend/storage
backend/public/backups

# Local-only compose overrides (host port, dev mounts, etc.) — never commit
docker-compose.override.yml
docker-compose.override.yaml

# Logs / coverage
*.log
npm-debug.log*
coverage
screenshots/
